import { Dialog, DialogContent, DialogHeader, DialogTitle } from './ui/dialog'

const LearningPathDialog = () => {
	return (
		<Dialog>
			<DialogContent>
				<DialogTitle></DialogTitle>
			</DialogContent>
		</Dialog>
	)
}
export default LearningPathDialog
